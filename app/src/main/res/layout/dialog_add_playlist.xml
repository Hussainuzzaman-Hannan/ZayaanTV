<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical" android:padding="20dp">

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="Playlist নাম:" android:textColor="#CCFFFFFF" android:textSize="13sp"
        android:layout_marginBottom="6dp"/>

    <EditText android:id="@+id/etPlaylistName"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:hint="যেমন: Sports Channels"
        android:textColorHint="#55FFFFFF" android:textColor="#FFFFFF"
        android:background="@drawable/search_bg" android:padding="12dp"
        android:inputType="text" android:layout_marginBottom="16dp"/>

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="M3U/M3U8 URL:" android:textColor="#CCFFFFFF" android:textSize="13sp"
        android:layout_marginBottom="6dp"/>

    <EditText android:id="@+id/etPlaylistUrl"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:hint="https://example.com/playlist.m3u8"
        android:textColorHint="#55FFFFFF" android:textColor="#FFFFFF"
        android:background="@drawable/search_bg" android:padding="12dp"
        android:inputType="textUri" android:layout_marginBottom="12dp"/>

    <!-- Hidden emoji recycler (placeholder) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEmoji"
        android:layout_width="match_parent" android:layout_height="0dp"
        android:visibility="gone"/>

    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="💡 URL পেস্ট করুন। চ্যানেলগুলো এই playlist-এ আলাদাভাবে সংরক্ষিত হবে।"
        android:textColor="#88FFFFFF" android:textSize="11sp" android:lineSpacingMultiplier="1.4"/>

</LinearLayout>